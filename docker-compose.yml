services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: davidwah-tailadmin-laravel:beta
        ports:
            - "8090:8090"
        environment:
            APP_ENV: production
            APP_DEBUG: "false"
            APP_URL: http://localhost:8090
            APP_KEY: ${APP_KEY:-}
            LOG_CHANNEL: stderr
            LOG_LEVEL: info
            DB_CONNECTION: mysql
            DB_HOST: mysql
            DB_PORT: 3306
            DB_DATABASE: tailadmin
            DB_USERNAME: ${{ secrete.DB_USER }}
            DB_PASSWORD: ${{ secrete.DB_PASS }}
            RUN_MIGRATIONS: "true"
        depends_on:
            mysql:
                condition: service_healthy
        restart: unless-stopped

    mysql:
        image: mysql:8.0
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: tailadmin
            MYSQL_USER: ${{ secrete.DBS_USER }}
            MYSQL_PASSWORD: ${{ secrete.DBS_PASS }}
        ports:
            - "3306:3306"
        volumes:
            - mysql-data:/var/lib/mysql
        healthcheck:
            test: [ "CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-proot" ]
            interval: 10s
            timeout: 5s
            retries: 10
            start_period: 20s
        restart: unless-stopped

volumes:
    mysql-data:
